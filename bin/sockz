#!/usr/bin/env node

const { Controller } = require('../dist');

const [cmd, src, role, ...args] = [...process.argv];
const ctl = new Controller(...args);

if (/agent/i.test(role)) {
  ctl.startAgent();
} else {
  ctl.startServer();
}
